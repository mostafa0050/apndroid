<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twofortyfouram.locale.platform"
>
    <application>
        <!--
            If a plug-in app does not stand alone (e.g. it is only a plug-in for Locale), including
            this in your manifest will help when users tap the "Open" button in the Android Market.
        -->
        <activity
            android:name="com.twofortyfouram.locale.MarketActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
        >
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />
                <category
                    android:name="android.intent.category.INFO" />
            </intent-filter>
        </activity>
    </application>
</manifest>